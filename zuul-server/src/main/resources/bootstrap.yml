spring:
  application:
    name: zuul-server
  cloud:
    consul:
      config:
        format: yaml
        fail-fast: ${FAIL_FAST:true}
      retry:
        initial-interval: 8000
        max-attempts: 6
        max-interval: 128000
        multiplier: 2
